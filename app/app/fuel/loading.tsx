import { ModuleSkeleton } from '@/components/Skeleton'

// Instant frame while Fuel's server data (macros · water · supplements) loads.
export default function FuelLoading() {
  return <ModuleSkeleton />
}
