import SocialsView from './SocialsView'

/**
 * Cross-brand socials view. Aggregates every linked account across every
 * brand into one screen — avatar, current follower count, and a small
 * trend sparkline pulled from each account's history.
 */
export default function SocialsPage() {
  return <SocialsView />
}
